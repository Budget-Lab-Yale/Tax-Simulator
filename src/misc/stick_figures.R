
stick_figures = function(tax_units){
  tax_units %>%
    mutate()
}

# Create bucket income
# Filter on conditions (filing status, SECA presence, etc)
# Group_by (pivot?)
# impute/aggregate
# Maybe we just need a few data points (inc, liab, characteristics)