# Agent instructions

**The instructions for this repository are in [`CLAUDE.md`](CLAUDE.md).** Read that
file; this one carries no content of its own.

Skills live in `.claude/skills/<name>/SKILL.md`, mirrored byte-identically into
`.agents/skills/`.

For the state-tax workstreams, start at [`research/README.md`](research/README.md) —
the index to plans, methods, evidence and decisions — and read the workstream's
`plan.md` before starting work.

---

*Why this is a stub.* `AGENTS.md` used to be a full copy of `CLAUDE.md`. The two
drifted: by 2026-08-19 the copy was a month stale, they disagreed on what
`states = all` resolves to, and the **State-Tax Development Protocol existed only
here** — so the file Claude Code actually reads never carried it. That protocol now
lives in `CLAUDE.md`. One file, no drift.
