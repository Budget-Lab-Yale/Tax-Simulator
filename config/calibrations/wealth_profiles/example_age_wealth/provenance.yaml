# Generated file -- written by other/wealth_dynamics/write_profiles.py.
#
# Provenance for the saving surface and transition matrix in this folder.
# The tables cannot carry their own metadata, so it lives here, in the
# same vocabulary the scalar calibration files use.
#
# The md5 of each table is recorded below, so that a table edited by hand
# after generation no longer matches its hash.

_channel:
  role: state

financing_profile:
  value: example_age_wealth
  kind: judgment
  set: '2026-06-24'
  tables:
    s.csv: 8f27b9f6601c95537678699134a93043
    M.csv: f69080edc998607de4a401f01cecc732
  note: >
    Illustrative rather than calibrated: a placeholder age by wealth-rank
    shape that exercises the bracket machinery and shows the file format. No
    scenario should ship results from it. The kind is judgment because there
    is no derivation to record.
