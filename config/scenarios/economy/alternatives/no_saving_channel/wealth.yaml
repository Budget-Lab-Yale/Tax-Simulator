# Saving-financing channel off, everything else at the model default. Used by the
# OBBBA retrospective smoke fixture, which is checking the reversion stack rather
# than the wealth channel.

financing_profile:
  value: none
