# Clausing corporate reform, on the interface version 5 vintage.
#
# The re-issue copies the original six columns unchanged and sets the static
# corporate column equal to the conventional one, so this scenario's static leg
# carries the corporate behavioral response. The reasoning is in the vintage's own
# corporate_meta.yaml.

off_model_estimates_vintage:
  value: '20260726'

off_model_estimates_id:
  value: 07_corporate
