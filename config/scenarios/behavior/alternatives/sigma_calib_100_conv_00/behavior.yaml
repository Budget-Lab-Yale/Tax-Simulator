# Generated file -- written by other/top_tax/sigma_calib/write_sigma_sweep.py.
#
# The sigma calibration stack at conv = 0.0. It is not a product stack: it
# reproduces the conditions sigma is derived under, which is why the charitable
# elasticity is named at charity/100 rather than inherited.
#
# That choice is load-bearing. The shipped sigma was derived under charity/100 where
# product runs use charity/50, and the mismatch is recorded as a dated waiver on the
# conversion.conv entry. Whichever this stack names is the basis of what the
# measurement writes.
#
# Named by: config/runscripts/tests/sigma_calib_100_conv_00.csv
# Measured by: other/top_tax/sigma_calib/measure_sigma.R
#
# Execution order is not the order below: src/sim/behavior.R sorts the list against
# one pinned family order, because later families read what earlier ones wrote.

kg_dynamics:
  bathtub: config/calibrations/kg/bathtub.yaml
  conversion: config/calibrations/kg/sweeps/conv_00/conversion.yaml

modules:
  - src/behavior/conversion/sigma.R
  - src/behavior/entity_shifting/pearce_prisinzano.R
  - src/behavior/evasion/debacker.R
  - src/behavior/charity/100.R
  - src/behavior/estate/avoidance.R
