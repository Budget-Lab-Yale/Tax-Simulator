# Generated file -- written by other/kg_model_tests/form_ab/write_bathtub_sweep.py.
#
# The top_tax_full stack with the bathtub bound to the timeable_share = 0.40 sweep
# point rather than to the shipped calibration. The rest of the stack is the
# product configuration, since the measurement means something only if the trial
# value is the one thing that differs.
#
# Named by: config/runscripts/tests/form_timeable_levels_40.csv
#
# The stack below is inherited from the top_tax_full alternative, comments and all:
#
# The full response stack used for the top-tax work: the gains bathtub, income
# conversion into the gain pool, entity shifting, income evasion, charitable
# giving at -0.5, and the estate reporting response. Every margin except the
# wealth-tax concealment, which only applies when there is a wealth tax to
# conceal from.
#
# Execution order is not the order below: src/sim/behavior.R sorts the list
# against one pinned family order, because some responses read what an earlier
# one wrote. See that file for the order and the reasons.

# kg_dynamics binds the pieces of the gains machinery this scenario uses, each to
# the calibration file carrying its value and provenance. The bathtub applier is
# not listed under modules: it is the machinery itself and comes in automatically.
# A sweep binds a different file of the same shape, which is how the sweep ends up
# recorded in the vintage.

kg_dynamics:
  bathtub: config/calibrations/kg/sweeps/timeable_share_40/bathtub.yaml
  conversion: config/calibrations/kg/conversion.yaml

modules:
  - src/behavior/conversion/sigma.R
  - src/behavior/entity_shifting/pearce_prisinzano.R
  - src/behavior/evasion/debacker.R
  - src/behavior/charity/50.R
  - src/behavior/estate/avoidance.R
