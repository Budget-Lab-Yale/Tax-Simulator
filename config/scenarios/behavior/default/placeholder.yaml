# Behavior leg -- default layer.
#
# The behavior leg's real contents (behavior.yaml: the kg_dynamics binding plus
# the list of module files a scenario runs) arrive with the behavior phase of
# the rebuild. Until then the leg resolves on this file so the engine has a
# default layer to load, and the runscript's behavior column still names
# modules directly.

_placeholder:
  value: none
  kind: structural
  note: >
    Placeholder so the behavior leg has a loadable default layer while the
    economy leg is the only one live. Read by nobody; removed when
    behavior.yaml lands.
